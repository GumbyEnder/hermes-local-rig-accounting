name: local-rig-accounting
version: 0.2.0
description: "Transparent per-token cost accounting for local LLM inference — depreciation, energy, throughput-based pricing displayed alongside cloud provider costs. Community leaderboard submissions via GitHub Issues."
author: GumbyEnder
kind: standalone
provides_tools:
  - rig_cost
  - rig_summary
  - rig_benchmark
  - rig_submit
provides_hooks:
  - post_api_request
  - on_session_finalize
  - on_session_start
